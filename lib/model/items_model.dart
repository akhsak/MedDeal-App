import 'package:flutter/material.dart';

class CategoryData {
  final String title;
  final IconData icon;
  final Color color;
  final Widget route;

  const CategoryData(this.title, this.icon, this.color, this.route);
}
